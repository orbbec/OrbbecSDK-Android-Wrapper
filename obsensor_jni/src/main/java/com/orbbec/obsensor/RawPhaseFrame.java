package com.orbbec.obsensor;

/**
 * \if English
 * @brief Define the RawPhaseFrame class, which inherits from the VideoFrame class
 * \else
 * TODO lumiaozi
 * \endif
 */
public class RawPhaseFrame extends VideoFrame {

    RawPhaseFrame(long handle) {
        super(handle);
    }
}
